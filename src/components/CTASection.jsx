import React from 'react';import { useNavigate } from 'react-router-dom';import { useAuth } from '../hooks/useAuth';const CTASection = () => {  const navigate = useNavigate();  const { user, token } = useAuth();  const handleScheduleViewing = async () => {    if (!user) {      navigate('/login');      return;    }    try {      const response = await fetch('/properties', {        method: 'POST',        headers: {          'Content-Type': 'application/json',          'Authorization': `Bearer ${token}`        },        body: JSON.stringify({          title: 'New Property',          location: 'City, Country',          price: 100000,          description: 'A beautiful new property',          imageURL: 'https://example.com/property.jpg'        })      });      if (response.ok) {        const property = await response.json();        console.log('Created property:', property);             } else {        const errorData = await response.json();        console.error('Error creating property:', errorData.error);      }    } catch (error) {      console.error('Error creating property:', error);          }  };  return (    <section className="bg-gradient-to-r from-blue-500 to-purple-600 text-white py-16" id="CTASection_1">      <div className="container mx-auto px-4">        <h2 className="text-4xl font-bold mb-4" id="CTASection_2">Ready to explore your new home?</h2>        <p className="text-xl mb-8" id="CTASection_3">Contact us today! See listings before they're gone!</p>        <button          className="bg-white text-blue-600 font-bold py-4 px-8 rounded-full text-xl hover:bg-blue-100 transition duration-300"          onClick={handleScheduleViewing}          id="CTASection_4"        >          Schedule a Viewing        </button>      </div>    </section>  );};export default CTASection;